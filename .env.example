# Prisma SQLite database URL.
# Relative path works in both local dev and CI (GitHub Actions).
# The db/ directory is gitignored — `bun run db:push && bun run db:seed`
# creates it fresh.
DATABASE_URL="file:./db/custom.db"
